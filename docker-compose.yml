version: '3.8'

services:
  vulncorp:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./documents:/app/documents
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
